[package]
name = "apx-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
